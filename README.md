# NIKE — Motion First
Full scaffold: Next.js (app router) + Tailwind CSS frontend + Strapi CMS + optional Firebase

Quick start:
1. Unzip
2. Copy .env.local.example to .env.local and fill values
3. Install dependencies: `npm install`
4. Run dev server: `npm run dev`
